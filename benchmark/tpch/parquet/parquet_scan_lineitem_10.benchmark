# name: benchmark/tpch/parquet/parquet_q1.benchmark
# description: Execute Q1 over lineitem stored in a parquet file
# group: [parquet]

name Q1 (Parquet)
group parquet
subgroup tpch

require parquet
require tpch

#load
#CALL dbgen(sf=10, suffix='_normal');
#COPY lineitem_normal TO '${BENCHMARK_DIR}/lineitem_10.parquet' (FORMAT PARQUET);

run
select * from read_parquet('${BENCHMARK_DIR}/lineitem_10.parquet');

