# name: ${FILE_PATH}
# description: ${DESCRIPTION}
# group: [clickbench]

require pixels

name Q${QUERY_NUMBER_PADDED}
group Clickbench

load
CREATE VIEW hits AS SELECT * FROM parquet_scan('/pm1725b-1/liyu/parquet_data/clickbench/*');

run benchmark/clickbench/queries-micro/q${QUERY_NUMBER_PADDED}.sql
